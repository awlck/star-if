# starvfs

Layered virtual filesystem (`docs/proposal.md` §2.1, §12.5). Backlog workstream G, Phase 0 -- independent of `stardata`, and the backlog notes it can be deferred entirely without affecting the phase exit criterion.

Not yet implemented. See `docs/proposal.md` §2.1 for the full repository
layout and `docs/phase-0-backlog.md` for what's scheduled when this
directory is not part of the current phase.
