# tests/corpus/invalid/removed-operator.star
# declared E-OP-REMOVED
# lexer and parser report E-OP-REMOVED

error: E-OP-REMOVED: '?=' was removed from the format
  --> tests/corpus/invalid/removed-operator.star:8:13
  |
8 |     notes   ?= "was this bound? you cannot tell from here, which was the problem"
  |             ^^
note: it meant "bind only if this key is unset", which could not be read locally: whether it bound depended on every other declaration of the key, at any inheritance level, in any file (spec §6.3.1)
note: the case it existed for is already covered -- a project loads after every library, so a library's plain `=` is always pre-emptable (spec §13.2)
fix-it: bind it plainly with '='
  replace tests/corpus/invalid/removed-operator.star:8:13 with `=`
