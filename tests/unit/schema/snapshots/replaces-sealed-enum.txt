# tests/corpus/invalid/replaces-sealed-enum.star
# loaded as a library, on top of the core-owned set and stdlib

error: E-SCHEMA-SEALED: 'advances_turn_enum' is sealed, so it can be added to but never replaced
  --> tests/corpus/invalid/replaces-sealed-enum.star:11:8
   |
11 | enum = @replaces(starcore) {
   |        ^^^^^^^^^^^^^^^^^^^
note: sealed means exactly this: extend freely, never supersede. Replacing it would change the shape of data starcore reads directly (spec §7.2.2, §7.6)
  --> libs/starcore/builtin/schema.star:87:5
   |
87 |     id     = advances_turn_enum
   |     ^^
