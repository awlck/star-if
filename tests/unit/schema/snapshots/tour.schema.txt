# tests/corpus/tour.star
# loaded as a library, on top of the core-owned set and stdlib
# backlog F9 / H5 -- see the note on this test in corpus_test.cpp

error: E-UNKNOWN-KEY: nothing declares 'calendar', so I don't know what this is
  --> tests/corpus/tour.star:116:1
    |
116 | calendar = {
    | ^^^^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: nothing declares 'calendar', so I don't know what this is
  --> tests/corpus/tour.star:136:1
    |
136 | calendar = {
    | ^^^^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: 'display_name' is not a key that 'sector' has
  --> tests/corpus/tour.star:478:5
    |
478 |     display_name = $sector_station_alpha
    |     ^^^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'neighbours' is not a key that 'sector' has
  --> tests/corpus/tour.star:479:5
    |
479 |     neighbours   = { docking_ring }
    |     ^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'on_deactivate' is not a key that 'sector' has
  --> tests/corpus/tour.star:480:5
    |
480 |     on_deactivate = serialize                # serialize | freeze | discard_changes | never
    |     ^^^^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'offstage' is not a key that 'sector' has
  --> tests/corpus/tour.star:481:5
    |
481 |     offstage = {
    |     ^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'display_name' is not a key that 'sector' has
  --> tests/corpus/tour.star:489:5
    |
489 |     display_name  = $sector_docking_ring
    |     ^^^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'neighbours' is not a key that 'sector' has
  --> tests/corpus/tour.star:490:5
    |
490 |     neighbours    = { station_alpha }
    |     ^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'on_deactivate' is not a key that 'sector' has
  --> tests/corpus/tour.star:491:5
    |
491 |     on_deactivate = serialize
    |     ^^^^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'offstage' is not a key that 'sector' has
  --> tests/corpus/tour.star:492:5
    |
492 |     offstage      = { model = catch_up }
    |     ^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'display_name' is not a key that 'sector' has
  --> tests/corpus/tour.star:500:5
    |
500 |     display_name  = $sector_wayfarer
    |     ^^^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'on_deactivate' is not a key that 'sector' has
  --> tests/corpus/tour.star:501:5
    |
501 |     on_deactivate = never
    |     ^^^^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'offstage' is not a key that 'sector' has
  --> tests/corpus/tour.star:502:5
    |
502 |     offstage      = { model = continuous  script = wayfarer_systems_tick }
    |     ^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'display_name' is not a key that 'sector' has
  --> tests/corpus/tour.star:508:5
    |
508 |     display_name  = $sector_surface
    |     ^^^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'local_clock' is not a key that 'sector' has
  --> tests/corpus/tour.star:509:5
    |
509 |     local_clock   = { calendar = kepler_iv  offset = "+04:30" }
    |     ^^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'on_deactivate' is not a key that 'sector' has
  --> tests/corpus/tour.star:510:5
    |
510 |     on_deactivate = serialize
    |     ^^^^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'offstage' is not a key that 'sector' has
  --> tests/corpus/tour.star:511:5
    |
511 |     offstage      = { model = simulate }     # expensive, correct, opt-in
    |     ^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-TYPE-MISMATCH: 'present_in' is declared set<ref<starcore.room>>, and this is a block
  --> tests/corpus/tour.star:774:18
    |
774 |     present_in = { where = { in_sector = station_alpha }  dynamic = no }
    |                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
note: set<ref<starcore.room>> takes a list of identifiers in braces (spec §6.2)

error: E-TYPE-MISMATCH: 'effects' is declared effect_block, and this is the identifier 'inherit'
  --> tests/corpus/tour.star:1103:18
     |
1103 |     effects    = inherit                # do not change the action's effects
     |                  ^^^^^^^
note: effect_block takes a block in braces (spec §6.2)

error: E-UNKNOWN-KEY: 'script' is not a key that 'rule' has
  --> tests/corpus/tour.star:1322:5
     |
1322 |     script    = handle_cursed_take
     |     ^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-TYPE-MISMATCH: 'effects' is declared effect_block, and this is the identifier 'inherit'
  --> tests/corpus/tour.star:1333:17
     |
1333 |     effects   = inherit
     |                 ^^^^^^^
note: effect_block takes a block in braces (spec §6.2)

error: E-UNKNOWN-KEY: 'phase' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1392:35
     |
1392 | turn_hook = { id = ss_upkeep      phase = upkeep      priority = 100
     |                                   ^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'priority' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1392:55
     |
1392 | turn_hook = { id = ss_upkeep      phase = upkeep      priority = 100
     |                                                       ^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'script' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1393:15
     |
1393 |               script = ss_tick_effects }
     |               ^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'phase' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1395:35
     |
1395 | turn_hook = { id = ss_initiative  phase = initiative  priority = 100
     |                                   ^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'priority' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1395:55
     |
1395 | turn_hook = { id = ss_initiative  phase = initiative  priority = 100
     |                                                       ^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'conditions' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1396:15
     |
1396 |               conditions = { any_actor = { in_combat == yes } }
     |               ^^^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'script' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1397:15
     |
1397 |               script = ss_roll_initiative }
     |               ^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'phase' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1399:35
     |
1399 | turn_hook = { id = ss_reaction    phase = reaction    priority = 100
     |                                   ^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'priority' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1399:55
     |
1399 | turn_hook = { id = ss_reaction    phase = reaction    priority = 100
     |                                                       ^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'script' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1400:15
     |
1400 |               script = ss_offer_reactions }
     |               ^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'phase' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1402:35
     |
1402 | turn_hook = { id = ss_round_end   phase = round_end   priority = 100
     |                                   ^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'priority' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1402:55
     |
1402 | turn_hook = { id = ss_round_end   phase = round_end   priority = 100
     |                                                       ^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'script' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1403:15
     |
1403 |               script = ss_end_of_round }
     |               ^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'phase' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1406:40
     |
1406 | turn_hook = { id = ss_combat_response  phase = round_start  priority = 90
     |                                        ^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'priority' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1406:61
     |
1406 | turn_hook = { id = ss_combat_response  phase = round_start  priority = 90
     |                                                             ^^^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: 'script' is not a key that 'turn_hook' has
  --> tests/corpus/tour.star:1407:15
     |
1407 |               script = ss_dispatch_combat_response }
     |               ^^^^^^
note: a form is closed unless it says otherwise, so that a mistyped key is caught rather than quietly ignored (spec §7.3)

error: E-UNKNOWN-KEY: nothing declares 'quest', so I don't know what this is
  --> tests/corpus/tour.star:1414:1
     |
1414 | quest = {
     | ^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: nothing declares 'quest', so I don't know what this is
  --> tests/corpus/tour.star:1453:1
     |
1453 | quest = {
     | ^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: nothing declares 'dialogue', so I don't know what this is
  --> tests/corpus/tour.star:1471:1
     |
1471 | dialogue = {
     | ^^^^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: nothing declares 'dialogue', so I don't know what this is
  --> tests/corpus/tour.star:1572:1
     |
1572 | dialogue = {
     | ^^^^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: nothing declares 'goal_def', so I don't know what this is
  --> tests/corpus/tour.star:1618:1
     |
1618 | goal_def = {
     | ^^^^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: nothing declares 'goal_def', so I don't know what this is
  --> tests/corpus/tour.star:1629:1
     |
1629 | goal_def = {
     | ^^^^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: nothing declares 'schedule', so I don't know what this is
  --> tests/corpus/tour.star:1649:1
     |
1649 | schedule = {
     | ^^^^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: nothing declares 'schedule', so I don't know what this is
  --> tests/corpus/tour.star:1660:1
     |
1660 | schedule = {
     | ^^^^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: nothing declares 'bark_table', so I don't know what this is
  --> tests/corpus/tour.star:1669:1
     |
1669 | bark_table = {
     | ^^^^^^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: nothing declares 'party', so I don't know what this is
  --> tests/corpus/tour.star:1682:1
     |
1682 | party = {
     | ^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-UNKNOWN-KEY: nothing declares 'lexicon', so I don't know what this is
  --> tests/corpus/tour.star:1709:1
     |
1709 | lexicon = {
     | ^^^^^^^
note: a top-level statement names either a form declared by a schema, or a class, in which case it creates one of them (spec §7.2, §7.4)

error: E-TYPE-MISMATCH: 'conditions' is declared condition_block, and this is the identifier 'inherit'
  --> tests/corpus/tour.star:1985:20
     |
1985 |     conditions   = inherit              # identical to omitting the statement
     |                    ^^^^^^^
note: condition_block takes a block in braces (spec §6.2)

error: E-TYPE-MISMATCH: 'restrictions' is declared condition_block, and this is the identifier 'inherit'
  --> tests/corpus/tour.star:1986:20
     |
1986 |     restrictions = inherit
     |                    ^^^^^^^
note: condition_block takes a block in braces (spec §6.2)

error: E-TYPE-MISMATCH: 'effects' is declared effect_block, and this is the identifier 'inherit'
  --> tests/corpus/tour.star:1987:20
     |
1987 |     effects      = inherit
     |                    ^^^^^^^
note: effect_block takes a block in braces (spec §6.2)

warning: W-PROVIDES-MISMATCH: starscape lists 'stat_block' in provides_schema, and declares no such form
  --> tests/corpus/tour.star:2017:5
     |
2017 |     provides_schema     = { stat_block combat_style loot_table }
     |     ^^^^^^^^^^^^^^^
note: `provides_schema` is a manifest, so the editor's library browser and a reader can see a library's forms in one place -- it declares nothing itself (spec §13.3)

warning: W-PROVIDES-MISMATCH: starscape lists 'combat_style' in provides_schema, and declares no such form
  --> tests/corpus/tour.star:2017:5
     |
2017 |     provides_schema     = { stat_block combat_style loot_table }
     |     ^^^^^^^^^^^^^^^
note: `provides_schema` is a manifest, so the editor's library browser and a reader can see a library's forms in one place -- it declares nothing itself (spec §13.3)

warning: W-PROVIDES-MISMATCH: starscape declares the form 'loot_entry', and does not list it in provides_schema
  --> tests/corpus/tour.star:2017:5
     |
2017 |     provides_schema     = { stat_block combat_style loot_table }
     |     ^^^^^^^^^^^^^^^
note: the manifest is what a reader and the editor's library browser go by, so a form missing from it is a form nobody finds (spec §13.3)
